apply plugin: "com.android.application"
apply plugin: "kotlin-android"
apply plugin: "kotlin-android-extensions"
android {
    buildToolsVersion "29.0.3"
    signingConfigs {
        key {
            keyAlias "Reduktor Demo"
            keyPassword "sroz3rzlgyinf75o"
            storeFile file("key")
            storePassword "l26a83xjk9zu3ujp"
        }
    }
    buildTypes {
        debug {
            minifyEnabled true
            multiDexEnabled false
            proguardFiles "proguard/debug.pro", "proguard/main.pro"
            shrinkResources true
            signingConfig signingConfigs.key
        }
        release {
            minifyEnabled true
            multiDexEnabled false
            proguardFiles "proguard/main.pro", "proguard/release.pro"
            shrinkResources true
            signingConfig signingConfigs.key
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    compileSdkVersion 29
    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 29
        versionCode 4
        versionName "2.0.0"
    }
}
androidExtensions {
    experimental = true
}
dependencies {
    implementation libraries.androidX.appCompat
    implementation libraries.androidX.recyclerView
    implementation libraries.androidX.swipeRefreshLayout
    implementation libraries.glide.core
    implementation libraries.glide.okHttp
    implementation libraries.gson
    implementation libraries.kotlin
    implementation libraries.material
    implementation libraries.okHttp
    implementation libraries.rxJava
    implementation libraries.schedulersFactory.android
    implementation libraries.schedulersFactory.core
    implementation project(":source:library")
}
repositories {
    google()
    jcenter()
    maven {
        url "https://jitpack.io"
    }
}