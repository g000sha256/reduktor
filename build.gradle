buildscript {
    apply from: "dependencies.gradle"
    dependencies {
        classpath gradlePlugin.android
        classpath gradlePlugin.kotlin
    }
    repositories {
        google()
        jcenter()
    }
}